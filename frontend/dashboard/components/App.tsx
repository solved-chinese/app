import React from "react";

type Props = {
  action: string;
  content: { qid: number };
};

export default class App extends React.Component<Props> {
    render(): JSX.Element {
        return <>hhh</>
    }
}